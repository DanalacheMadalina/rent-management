<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.ase.proiectgestiune">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->


        <activity
            android:name=".bun.VizualizareMap"
            android:label="@string/title_activity_vizualizare_map"></activity>
        <activity
            android:name=".bun.Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".bun.fragment.ListaApartamente" />
        <activity android:name=".bun.fragment.DetaliiClienti" />
        <activity android:name=".bun.ItemDetailsLV_ap_Libere" />
        <activity android:name=".bun.AdaugaApartament" />
        <activity android:name=".bun.Lista_Ap_Libere" />
        <activity android:name=".bun.Lista_Ap_Ocupate" />
        <activity android:name=".bun.fragment.ListaClienti" />
        <activity android:name=".bun.fragment.Profit" />
        <activity android:name=".bun.fragment.Cheltuieli" />
        <activity android:name=".bun.AdaugaCheltuiala" />
        <activity android:name=".bun.ListaCheltuieli" />
        <activity
            android:name=".bun.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBsDex85xV5KzvTahsE1RcxNQKUIV0rJ3E" />
        <uses-library android:name="org.apache.http.legacy" android:required="false" />
    </application>

</manifest>